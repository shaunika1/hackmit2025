import { useState, useEffect} from 'react';

export default function BMI(){
    const [data, setData] = useState(null);

    // useEffect(() => {
    //     fetch("")
    //     .then(res => res.)
    // })

return(
    <>
    <div className = "text-center p-96 text-red-500">
    <p>Your BMI IS:</p>
    </div>
    </>
);
}